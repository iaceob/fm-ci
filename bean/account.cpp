#include "account.h"

Account::Account() {
    id = "-1";
}

//QString Account::toString() {
//    return QString("ID: %1, name: %2, email: %3, stat: %4")
//            .arg(id).arg(name).arg(email).arg(stat);
//}
